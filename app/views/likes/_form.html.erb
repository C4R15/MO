<%= form_for(@like) do |f| %>
  <% if @like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@like.errors.count, "error") %> prohibited this like from being saved:</h2>

      <ul>
      <% @like.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name_fb_id %><br>
    <%= f.text_field :name_fb_id %>
  </div>
  <div class="field">
    <%= f.label :page_fb_id %><br>
    <%= f.text_field :page_fb_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
